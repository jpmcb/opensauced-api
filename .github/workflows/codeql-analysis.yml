# Disabling the code scanning until we have enabled GitHub advanced security

name: CodeQL

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "56 5 * * 1"

jobs:
  analyze:
    uses: open-sauced/hot/.github/workflows/codeql-analysis.yml@main
